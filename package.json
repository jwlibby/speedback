{
  "name": "speedback",
  "version": "1.1.0",
  "description": "Generates pair rotations schedule for speed dating style feedback sessions.  X number of people, in one session.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mcasseljlibby",
  "license": "ISC",
  "dependencies": {
  }
}
